const { StyleSheet } = require("react-native");
const { PADDING } = require("../../utils/constantes");

const styles = StyleSheet.create({
    root: {
        paddingVertical: PADDING.vertical,
        paddingHorizontal: PADDING.horizontal
    }
})

export default styles